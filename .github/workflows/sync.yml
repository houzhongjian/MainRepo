#name: Sync Split Repositories
#
#on:
#  push:
#    branches:
#      - main
#
#jobs:
#  sync-sdk:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Push sdk to repo-b
#        run: |
#          git config --global user.name "github-actions"
#          git config --global user.email "actions@github.com"
#
#          git subtree split --prefix=sdk -b split-sdk
#
#          git push https://${{ secrets.REPO_B_TOKEN }}@github.com/yourname/repo-b.git split-sdk:main --force
#
#  sync-api:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v4
#
#      - name: Push api to repo-c
#        run: |
#          git config --global user.name "github-actions"
#          git config --global user.email "actions@github.com"
#
#          git subtree split --prefix=api -b split-api
#
#          git push https://${{ secrets.REPO_C_TOKEN }}@github.com/yourname/repo-c.git split-api:main --force